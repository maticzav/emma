import { Input } from './Input'

describe('InfiniteSelect component works correctly', () => {
  test('Keypress up moves up.', async () => {
    expect(true).toBe(false)
  })
  test('Keypress down moves down.', async () => {
    expect(true).toBe(false)
  })

  test('Enter triggers onEnter callback', async () => {
    expect(true).toBe(false)
  })

  test('Placeholder is correctly applied', async () => {
    expect(true).toBe(false)
  })
})
