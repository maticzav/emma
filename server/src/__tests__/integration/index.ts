describe('Emma sets up', () => {
  test('Sends PR on new installed repository', async () => {
    expect(2).toBe(1)
  })
  test('Watches for configuration', async () => {
    expect(2).toBe(1)
  })
})
